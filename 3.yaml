---
- name: here comes the files to deploy cluster
  hosts: all
  remote_user: mukunth
  become: true
  tasks:
    - name: here we install contrack tool
      apt:
        name: conntrack
        state: present
   
    - name: here comes the first play to deploy pod
      command: kubectl create -f deployment.yaml
      args:
        chdir: /home/mukunth/ansible
    - name: here we dploy the services
      command: kubectl create-f services.yaml
      args:
        chdir: /home/mukunth/ansible
...
    
